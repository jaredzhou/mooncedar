name = "jaredzhou/mooncedar"

version = "0.1.4"

license = "Apache-2.0"

repository = "https://github.com/jaredzhou/mooncedar"

readme = "README.md"

description = "A Cedar policy engine — parser, evaluator, and authorizer in MoonBit."

preferred_target = "native"

import {
  "moonbitlang/x@0.4.45",
}
